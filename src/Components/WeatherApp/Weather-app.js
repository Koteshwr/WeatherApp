import '../WeatherApp/Weather-app.css';
import Panel from '../Panel/Panel';

const WeatherApp = () => {
    return (
        <div className="weather-app" >
            <Panel />
        </div>
    )
};
export default WeatherApp;